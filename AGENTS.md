# AI Context Harness Rules

このファイルは、AI Context Harness を利用・更新する AI エージェントが必ず守る絶対ルールです。

## 1. Context対象の判定

1. 新しいチャットを開始しただけでは、どのプロジェクトにも自動的に紐付けない。
2. プロジェクト専用チャットから開始することを前提にしない。通常の新規チャットからでもプロジェクトを復元できる設計とする。
3. ユーザーの発言・提示されたリポジトリ・明示されたプロジェクト名・作業内容から対象プロジェクトを判定する。
4. 明確に対象プロジェクトが特定できる場合のみ、そのプロジェクトの Context を読み込む。
5. 複数プロジェクトに該当し得る場合は、勝手に決定せずユーザーへ確認する。
6. プロジェクトとの関連が弱い一般質問・雑談・日常会話では Context を読み込まない。
7. 「関連していそう」という理由だけで、会話を既存プロジェクトへ紐付けてはならない。
8. ユーザーが明示的に「このプロジェクトとして扱う」と指定した場合は、その指定を優先する。

## 2. Project Identity Resolution

1. 対象リポジトリが分かる場合、`projects/README.md` の Project Registry から Project ID を解決する。
2. 対象リポジトリ側に `AI_CONTEXT.md` が存在する場合は、それを優先的な識別情報として利用する。
3. Project ID が確定したら、`projects/<project-id>/CONTEXT.md`、`STATE.md` を読み込む。
4. 過去の判断が作業に影響する場合は `DECISIONS.md` も読み込む。
5. Context と実際の対象リポジトリの内容に矛盾がある場合、現在の対象リポジトリを最新の事実として扱い、Context の更新要否を判断する。

## 3. Repository Visibility Policy（絶対ルール）

1. リポジトリの公開・非公開判断、Contextへの情報保存、プロジェクト登録を行う前に、必ず `global/repository-policy.md` を参照する。
2. `global/repository-policy.md` の公開境界をハード制約として扱い、AIの独自判断で緩和してはならない。
3. 対象リポジトリがRegistryに未登録の場合は、実際のGitHubリポジトリの公開状態を確認してからContextを保存する。
4. 公開状態を確認できない場合は、Private相当として扱い、公開可能性が確認されるまで個人情報・機密情報・非公開情報をContextへ保存しない。
5. 新しいPublicプロジェクトをRegistryへ追加する際は、Project ID・Repository・Purpose・Context・公開可能性を確認する。
6. Public Context に個人・顧客・勤務先・秘密・非公開案件に関する情報を混在させない。
7. Publicリポジトリには、公開を前提としてレビューした情報のみ保存する。

## 4. Work / Context Separation

1. 実際のコード・アプリ・ドキュメント・Issue等の作業は対象プロジェクトのリポジトリで行う。
2. AI Context Harness は、公開可能なプロジェクトについては継続状態・判断履歴・再利用可能なコンテキストを管理する。
3. Private プロジェクトの詳細な継続コンテキストは、この Public Harness に保存しない。
4. Context Harness にコード本体をコピーして管理してはならない。
5. プロジェクト作業完了後、公開可能な範囲で継続に必要な状態を Context Harness に反映する。

## 5. State Update Rules

1. 意味のあるプロジェクト作業を完了したら、対象プロジェクトの `STATE.md` を更新する。
2. `STATE.md` には少なくとも「現在の状態」「直近で完了したこと」「次の具体的なアクション」を残す。
3. 新しい重要な判断を行った場合は `DECISIONS.md` に記録する。
4. 長期的に有効な前提・知識が変わった場合のみ `CONTEXT.md` を更新する。
5. 作業が完了していない場合、完了したかのように `STATE.md` を更新してはならない。

## 6. No Unwanted Tracking

1. 普通のChatGPT利用をプロジェクト活動として記録してはならない。
2. ユーザーが明示していない個人的な会話・雑談を Context Harness に保存してはならない。
3. プロジェクトに関係しない情報を既存プロジェクトの Context に混入させてはならない。
4. AI Context Harness はユーザーの全会話履歴を保存する場所ではない。

## 7. Security

1. Secrets、API keys、passwords、tokens、credentials を絶対に保存しない。
2. 顧客情報・勤務先の機密情報・個人情報・非公開情報を保存しない。
3. 公開リポジトリに置かれる可能性のある情報は、公開可能性を前提に安全性を確認する。
4. 不要な個人情報は保存しない。

## 8. History Integrity

1. `DECISIONS.md` の過去の判断を、履歴を壊す形で書き換えてはならない。
2. 判断を変更した場合は、変更理由と変更日を新しい記録として残す。
3. 過去の状態と現在の状態を混同しない。

## 9. AI Work Policy

モデル選択とトークン消費については、`global/ai-work-policy.md` を共通ルールとして適用する。

1. 作業開始時は、まず軽量な探索・検索で対象範囲と難易度を確認する。
2. 単純な作業は軽量モデルを優先し、複雑な設計・分析・重要判断では高性能モデルを使用する。
3. 軽量モデルで解決できない場合のみ、高性能モデルへの切り替えを検討する。
4. 必要なファイル・行・関連コードだけを読み、同じ情報を繰り返し取得しない。
5. トークン削減を理由に必要な調査・検証・安全確認を省略してはならない。
6. 特定モデル名・提供仕様をContextの固定値として保存しない。

## 10. Git Work Policy（絶対ルール）

Git / GitHub 作業については、`global/git-work-policy.md` を共通ルールとして適用する。

1. 意味のある変更は原則として `変更 → 検証 → commit → GitHub反映確認` までを作業単位とする。
2. ローカルcommitだけでは作業完了・Push完了と判断しない。
3. `git push` が実行環境のネットワーク・認証・サンドボックス制約で失敗した場合、利用可能なGitHub連携/API経路で反映を試行する。
4. GitHubへの反映を確認できるまで「Push済み」と報告しない。
5. 反映できない場合は、commit SHA、対象ブランチ、失敗理由を明示する。
6. 原則としてforce pushを行わない。

## 11. Completion Rule

プロジェクト作業を「完了」と判断する前に、必要に応じて以下を確認する。

- [ ] 対象プロジェクトが正しく特定されている
- [ ] 対象リポジトリの作業が完了している
- [ ] `STATE.md` が現在の状態を反映している
- [ ] 重要な判断が `DECISIONS.md` に記録されている
- [ ] 長期的な前提変更が `CONTEXT.md` に反映されている
- [ ] リポジトリ公開・非公開ポリシーに違反していない
- [ ] 不要な個人情報・秘密情報が保存されていない
- [ ] モデル選択・トークン最適化ルールに違反していない
- [ ] GitHubへの反映状態を確認している

## 12. Default Behavior

対象プロジェクトが不明な場合のデフォルト動作は「通常チャット」です。

AI Context Harness を使うこと自体を目的化せず、**必要なときだけ Context を読み、必要なときだけ Context を更新する**。
